language: rust

jobs:
    fast_finish: true
    include:
        - rust: stable
          script:
            - cargo build
            - ifconfig
          # cache ~/.cargo explicitly so that ./target isn't cached
          cache:
              directories:
                  - $HOME/.cargo
